import Foundation

/// Переносит замыкание от контроллера к вью, созданной до него.
///
/// Вью строится в инициализаторе, когда `self` ещё нельзя захватывать;
/// коробка позволяет подставить обработчик позже, не пересобирая иерархию.
@MainActor
final class FrameBox {
    var handler: ((CGSize) -> Void)?
}
